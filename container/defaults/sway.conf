default_border none
default_floating_border none
gaps inner 0
gaps outer 0
focus_follows_mouse yes
focus_wrapping no
workspace_auto_back_and_forth no

# KWin's nested Wayland output windows are positioned by the WebKDE control
# handler through sway IPC.
for_window [title=".*WL-[0-9]+.*"] floating enable, border none
